<figure class="meta-thumb zoomable mob-12 tab-6 dsk-3">
    <figcaption class="centered">
        {{ link.ingredient.name }} ({{ link.quantity }} {{ link.ingredient.unit }})
    </figcaption>
    <img class="thumb" src="{{ asset('uploads/img/ingredient/'~link.ingredient.illustration) }}">
</figure>
