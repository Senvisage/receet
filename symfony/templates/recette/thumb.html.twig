<figure class="meta-thumb zoomable mob-12 tab-6 dsk-3">
    <figcaption class="centered">
        {{ link.recette.name }} ({{ link.quantity }} {{ ingredient.unit }})
    </figcaption>
    <img class="thumb" src="{{ asset('uploads/img/recette/'~link.recette.illustration) }}">
</figure>
